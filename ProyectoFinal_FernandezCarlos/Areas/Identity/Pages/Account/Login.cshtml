@page
@model LoginModel

@{
    ViewData["Title"] = "Inicio de Sesión";
}

<a asp-controller="Home" asp-action="Inicio" class="back-link">Atrás</a>

<div class="form-page">
    <h1>Inicio de Sesión</h1>

    <form id="account" method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>

        <div class="form-floating mb-3">
            <input asp-for="Input.Email" class="form-control" autocomplete="username" aria-required="true" placeholder="correo@ejemplo.com" />
            <label asp-for="Input.Email">Correo electrónico</label>
            <span asp-validation-for="Input.Email" class="text-danger"></span>
        </div>

        <div class="form-floating mb-3">
            <input asp-for="Input.Password" class="form-control" autocomplete="current-password" aria-required="true" placeholder="contraseña" />
            <label asp-for="Input.Password">Contraseña</label>
            <span asp-validation-for="Input.Password" class="text-danger"></span>
        </div>

        <div class="checkbox mb-3">
            <label asp-for="Input.RememberMe" class="form-label">
                <input class="form-check-input" asp-for="Input.RememberMe" />
                Recordarme
            </label>
        </div>

        <button id="login-submit" type="submit" class="w-100 btn btn-primary mb-3">
            Iniciar sesión
        </button>

        <a asp-page="./Register" asp-route-returnUrl="@Model.ReturnUrl" class="w-100 btn btn-primary">
            Registrar estudiante nuevo
        </a>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}